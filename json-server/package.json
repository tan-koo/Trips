{
  "name": "json-server",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port 9000"
  },
  "dependencies": {
    "express": "^4.17.1",
    "json-server": "^0.16.1"
  }
}
